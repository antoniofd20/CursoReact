import React from 'react';

// Escribir sfc para una función.
// Si no se necesita código js se puede omitir las llaver y el return.
const Footer = ({fecha}) => ( 
<p>Todos los rderechos reservados &copy; { fecha }</p>
);
 
export default Footer;